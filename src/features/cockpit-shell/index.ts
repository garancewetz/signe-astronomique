// Public API for the cockpit-shell feature: the orchestrating Cockpit
// component, the React 19 ErrorBoundary that wraps it, the floating shell
// chrome (sidebar, mobile sheet, body-info HUD, legend), and the
// keyboard-shortcut helpers.

export { Cockpit } from './Cockpit';
export { ErrorBoundary } from './ErrorBoundary';
export { CockpitFallback } from './CockpitFallback';
